# Backend API URL
VITE_API_URL=http://localhost:8000/api

# Temporary auth token for MVP (until auth UI is built)
# To generate:
# 1. Start backend: cd backend && uv run uvicorn app.main:app --reload
# 2. Register: POST http://localhost:8000/api/auth/register with {"username":"test","email":"test@example.com","password":"test123"}
# 3. Login: POST http://localhost:8000/api/auth/login with {"username":"test","password":"test123"}
# 4. Copy the access_token from response and paste below
VITE_AUTH_TOKEN=your_jwt_token_here
